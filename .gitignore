# dependencies
node_modules
.venv
.pnp
.pnp.js

# build
.next
out
build
dist

# misc
.DS_Store
*.pem
.env*.local
.vercel

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# types
next-env.d.ts

# 題庫來源 PDF（體積大，不推上 GitHub）
/raw_pdfs:/
raw_pdfs:/

# build 時由 verify_data_integrity 產出，不提交
public/data/verify_result.json
public/data/multi/verify_result_multi.json

# approval:bundle:multi 產出之「合併前 comment」填寫稿（含本機 commit，不提交）
scripts/pr_comment_merge_check_multi.md

# 本機匯入暫存（體積大或每次不同，不提交）
scripts/backup/
scripts/parser_debug/
scripts/import_last_run.log
scripts/import_last_run_multi.log
scripts/parser_debug_multi/
# multi 匯入報告（每次匯入產生，不提交；與 Phase 2A PR 範圍一致）
scripts/import_report_multi.json
